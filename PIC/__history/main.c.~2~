#include <main.h>
#fuses XT,NOWDT,NOPROTECT,NOBROWNOUT,NOLVP,NOPUT,NOWRT,NODEBUG,NOCPD
#use delay(clock=4M)
void main()
{
   set_tris_b(0x00);
   while(TRUE)
   {
      output_high(pin_b0);
      delay_ms(500);
      output_low(pin_b0);
      delay_ms(500);
   }

}
